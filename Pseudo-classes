<!DOCTYPE html>
<html>
<head>
  <style>
    * {
      font-family: 'Arial', sans-serif;
      box-sizing: border-box;
    }
    
    body {
      max-width: 800px;
      margin: 0 auto;
      padding: 20px;
    }

    /* การตั้งค่า Section แต่ละส่วน */
    .section {
      margin: 30px 0;
      padding: 20px;
      border: 1px solid #eee;
      border-radius: 8px;
    }

    /* การตั้งค่า Hover Effect เมื่อเอาเมาส์ทับปุ่ม */
    .hover-button {
      padding: 10px 20px;
      background-color: #4CAF50;
      color: white;
      border: none;
      border-radius: 4px;
      cursor: pointer;
      transition: all 0.3s;
    }

    /* เปลี่ยนสีพื้นหลังและขยายขนาดเล็กน้อยเมื่อ Hover */
    .hover-button:hover {
      background-color: #45a049;
      transform: scale(1.05);
    }

    /* การตั้งค่า Focus Effect เมื่อโฟกัสที่ช่องกรอกข้อมูล */
    .focus-input {
      padding: 8px;
      border: 2px solid #ddd;
      border-radius: 4px;
      width: 200px;
    }

    /* เปลี่ยนสีเส้นขอบและแสดงเงาเมื่อมีการ Focus */
    .focus-input:focus {
      outline: none;
      border-color: #4CAF50;
      box-shadow: 0 0 5px rgba(76, 175, 80, 0.5);
    }

    /* การตั้งค่าสำหรับ List แสดงรายการ */
    .demo-list {
      list-style: none;
      padding: 0;
    }

    /* ตกแต่งรายการใน List แต่ละลำดับ */
    .demo-list li {
      padding: 10px;
      border: 1px solid #ddd;
      margin: 5px 0;
    }

    /* สีพื้นหลังและตัวหนาสำหรับรายการแรก */
    .demo-list li:first-child {
      background-color: #e8f5e9;
      font-weight: bold;
    }

    /* สีพื้นหลังสำหรับรายการสุดท้าย */
    .demo-list li:last-child {
      background-color: #fce4ec;
    }

    /* สีพื้นหลังเฉพาะรายการคู่ */
    .demo-list li:nth-child(even) {
      background-color: #f5f5f5;
    }

    /* การตั้งค่าสำหรับ Checkbox และ Label เมื่อถูกเช็ค */
    .checkbox-demo {
      margin: 10px 0;
    }

    /* เปลี่ยนสีและตัวหนาของ Label เมื่อ Checkbox ถูกเลือก */
    .checkbox-demo input[type="checkbox"]:checked + label {
      color: #4CAF50;
      font-weight: bold;
    }

    /* การตั้งค่าสำหรับคำพูดที่ใช้ ::before และ ::after */
    .quote {
      color: #666;
      font-style: italic;
    }

    /* แสดงเครื่องหมายคำพูดด้านหน้าและหลังข้อความ */
    .quote::before {
      content: "「";
      color: #4CAF50;
    }
    .quote::after {
      content: "」";
      color: #4CAF50;
    }

    /* การตั้งค่าสำหรับการตรวจสอบข้อมูลในฟอร์ม */
    .validation-input {
      padding: 8px;
      border: 2px solid #ddd;
      border-radius: 4px;
      width: 200px;
      margin: 5px 0;
    }

    /* เปลี่ยนสีเส้นขอบเมื่อข้อมูลถูกต้อง */
    .validation-input:valid {
      border-color: #4CAF50;
    }

    /* เปลี่ยนสีเส้นขอบเมื่อข้อมูลไม่ถูกต้อง */
    .validation-input:invalid {
      border-color: #f44336;
    }

    /* การตั้งค่า Disabled Button */
    .disabled-button {
      padding: 10px 20px;
      background-color: #4CAF50;
      color: white;
      border: none;
      border-radius: 4px;
      cursor: pointer;
    }

    /* เปลี่ยนสีและทำให้ไม่สามารถกดได้เมื่อ Disabled */
    .disabled-button:disabled {
      background-color: #cccccc;
      cursor: not-allowed;
    }

  </style>
</head>
<body>
  <h1>CSS Pseudo-selectors Demo</h1>

  <!-- Section สาธิตการใช้งาน Hover Effect -->
  <div class="section">
    <h3>1. Hover Effect (:hover)</h3>
    <button class="hover-button">Hover ทับฉันสิ!</button>
  </div>

  <!-- Section สาธิตการใช้งาน Focus Effect -->
  <div class="section">
    <h3>2. Focus Effect (:focus)</h3>
    <input type="text" class="focus-input" placeholder="คลิกเพื่อดู focus effect">
  </div>

  <!-- Section สาธิตการใช้งาน List Selectors -->
  <div class="section">
    <h3>3. List Selectors (:first-child, :last-child, :nth-child)</h3>
    <ul class="demo-list">
      <li>รายการที่ 1 (first-child)</li>
      <li>รายการที่ 2</li>
      <li>รายการที่ 3</li>
      <li>รายการที่ 4</li>
      <li>รายการที่ 5 (last-child)</li>
    </ul>
  </div>

  <!-- Section สาธิตการใช้งาน Checkbox -->
  <div class="section">
    <h3>4. Checkbox with Label (:checked)</h3>
    <div class="checkbox-demo">
      <input type="checkbox" id="check1">
      <label for="check1">เช็คบ็อกซ์นี้เพื่อเปลี่ยนสี</label>
    </div>
  </div>

  <!-- Section สาธิตการใช้งานเครื่องหมายคำพูดด้วย ::before และ ::after -->
  <div class="section">
    <h3>5. Quotes with ::before และ ::after</h3>
    <p class="quote">นี่คือข้อความที่มีเครื่องหมายคำพูดแบบญี่ปุ่น</p>
  </div>

  <!-- Section สาธิตการตรวจสอบข้อมูลในฟอร์ม -->
  <div class="section">
    <h3>6. Form Validation (:valid, :invalid)</h3>
    <input type="email" class="validation-input" placeholder="ใส่อีเมล์" required>
  </div>

  <!-- Section สาธิตการใช้ Disabled Button -->
  <div class="section">
    <h3>7. Disabled State (:disabled)</h3>
    <button class="disabled-button">ปุ่มปกติ</button>
    <button class="disabled-button" disabled>ปุ่มที่ถูก Disabled</button>
  </div>

</body>
</html>
